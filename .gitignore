# 1. 시스템 및 캐시 파일 (OS & IDE)
#####################################
.DS_Store
Thumbs.db
.vscode/
.idea/

# 2. Python 환경 및 컴파일 파일
#####################################
# 가상 환경
venv/
env/
# 파이썬 컴파일 및 캐시
__pycache__/
*.py[cod]
*$py.class


# 3. 대용량 데이터 및 모델 (Git 추적 제외 필수)
#####################################
# 데이터셋 폴더 전체 제외 (VOC 데이터 및 변환 결과)
Dataset/
Datasets/
VOC2012/
VOCdevkit/

# 압축 파일
*.zip
*.tar
*.tar.gz

# 학습된 가중치 파일 (.pth) 폴더 전체 제외
weights/
*.pth
*.pt


# 4. 임시 로그 및 결과 파일 (Git 추적 제외)
#####################################
# 학습 중 매 에폭마다 갱신되는 임시 로그 파일 제외
train_log.txt

# predict.py 실행 시 생성되는 임시 결과 이미지 제외
result.jpg
*.png
*.jpeg


# 5. 최종 결과물 폴더 (Git 추적 포함!)
#####################################
# 위에서 모든 폴더를 제외시켰더라도, 아래 폴더는 추적을 강제하여 최종 보고서 자료를 보관
!report_assets/
!results/
!results/**